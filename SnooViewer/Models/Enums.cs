namespace SnooViewer.Models
{
    public class Enums
    {
        public enum Lang { En, Es };
        public enum FlairPosition { Empty, Left, Right };
        public enum NotificationLevel { Low };
        public enum SubmissionType { Any, Link, Self };
        public enum SubredditType { Public };
        public enum UserFlairType { Richtext, Text };
        public enum WhitelistStatus { AllAds, NoAds };
        public enum KindEnum { T5 };
    }
}
